#Declare a variable called hero and assign it the value "$$$Superman$$$"
#Use the strip() method to remove the $ symbols from the string
#Then print the value of hero to the console

# Strip the $ symbols from the string stored in the variable hero
hero = "$$$Superman$$$"
hero = hero.strip("$")

# Print the modified string
print(hero)